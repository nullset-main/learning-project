[Machine Learning]
name = "learning-project"
version = "0.1.0"
description = "objective: to learn ml, dl, ai"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.1.1",
]

[dependency-groups]
dev = [
    "ipykernel>=7.0.0",
]
